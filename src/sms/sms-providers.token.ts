/**
 * Token d'injection du tableau des fournisseurs SMS transactionnels
 * (`ManagedSmsProvider[]`), fourni par une factory dans `SmsModule`.
 */
export const SMS_PROVIDERS = Symbol('SMS_PROVIDERS');
